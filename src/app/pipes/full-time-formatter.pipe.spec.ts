import { FullTimeFormatterPipe } from './full-time-formatter.pipe';

describe('FullTimeFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new FullTimeFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
